export enum OrderStatus {
  PENDING = "pending",
  CONFIRMED = "confirmed",
  DELIVERED = "delivered",
  CANCELLED = "cancelled",
}

export enum OrderType {
  CUSTOMER = "customer",
  SUPPLIER = "supplier",
}
    