export enum MaterialEntryTypes {
  PURCHASE = "purchase",
  PRODUCTION = "production",
  RETURN = "return",
  SALES = "sales",
  TRANSFER = "transfer",
  USAGE = "usage",
}

export enum MaterialMovementTypes {
  IN = "in",
  OUT = "out",
  TRANSFER = "transfer",
  ADJUSTMENT = "adjustment",
}

export enum MaterialUnitTypes {
  PIECE = "piece",
  KG = "kg",
  LITER = "liter",
  BALE = "bale",
}
